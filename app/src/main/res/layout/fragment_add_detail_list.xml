<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_add_detail_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.nuwan.epicure.FRAGMENT.add_detail_list_fragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/layout_description_add_detail_list">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="35sp"
            android:text="Incomplete Disease Reports"
            android:layout_marginTop="7sp"
            android:textSize="15sp"
            android:id="@+id/tv_description_add_detail"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="35sp"
            android:id="@+id/btn_cancel_add_detail_list"
            android:text="Cancel"
            android:layout_toRightOf="@+id/tv_description_add_detail"
            android:layout_marginLeft="6sp"/>
    </RelativeLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/layout_description_add_detail_list">

        <ListView
            android:paddingTop="15dp"
            android:id="@+id/lstDisease"
            android:layout_width="match_parent"
            android:dividerHeight="@dimen/activity_vertical_margin"
            android:divider="#00000000"
            android:layout_height="505dip">
        </ListView>
    </FrameLayout>
</RelativeLayout>
